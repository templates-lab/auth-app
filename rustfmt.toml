# Shared formatting rules for every crate in the workspace.
# Only stable rustfmt options are used so `cargo fmt` needs no nightly toolchain.
edition = "2021"
max_width = 100
newline_style = "Unix"
use_small_heuristics = "Default"
use_field_init_shorthand = true
